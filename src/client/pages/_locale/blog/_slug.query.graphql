query ($locale: SiteLocale, $slug: String)  {
  page: blogPost(locale: $locale, filter: { slug: { eq: $slug } }) {
    ...page
  },
  alternate: blogPost(locale: $locale, filter: { slug: { eq: $slug } }) {
    slug
  },
  alternateParent: blogPostOverview {
    slug
  }
}
fragment page on BlogPostRecord {
  slug,
  title,
  subtitle,
  headerIllustration {
    url,
    alt
  },
  date: publishDate,
  authors {
    name,
    lastName,
    image {
      url,
      alt,
      width,
      height
    }
  }
  introTitle,
  items {
    __typename,
     ... on TextSectionRecord {
      title,
      body,
    },
    ... on ImageWithTextRecord {
      imageWithDescription {
        image {
          url,
          alt,
          width,
          height,
          format
        },
        description,
        inverse
      }
    },
    ... on ImageRecord {
      image {
        url,
        alt,
        width,
        height,
        format
      },
      alt,
      fullWidth,
    },
    ... on LinkRecord {
    	link,
      label,
      external,
    },
  },
  callToActionTitle,
  callToActionBody,
  callToActionLabel,
  socialTitle
}
