query ($locale: SiteLocale)  {
  page: blogPostOverview(locale:$locale) {
    ...blogPostOverview
  },
  items: allBlogPosts(locale: $locale) {
    slug
  },
  alternate:blogPostOverview {
    slug
  },
  latestBlogposts: allBlogPosts(locale:$locale, orderBy: publishDate_DESC) {
    slug,
    title,
    published,
    date: publishDate,
    authors {
      name,
      image {
        url,
        width,
        height,
        format
      }
    }
  }
}

fragment blogPostOverview on BlogPostOverviewRecord {
  title,
  slug,
  subtitle,
  description,
  headerIllustration {
    url,
    alt
  }
  callToActionLabel,
}
