query ($locale: SiteLocale)  {
  page: contact(locale: $locale) {
    ...contact
  },

  alternate: contact(locale: $locale) {
    slug
  },

  items: allContactConfirmations(locale: $locale) {
    slug
  },
}

fragment contact on ContactRecord {
  slug,
  subtitle,
  social {
    description
  },
  keywords,
  contactPageForm {
    subjectTitle,
    contactTitle,
    nameLabel,
    namePlaceholder,
    nameErrorMessage,
    emailLabel,
    emailPlaceholder,
    emailErrorMessageEmpty,
    emailErrorMessageIncorrect,
    businessLabel,
    businessPlaceholder,
    phoneLabel,
    phonePlaceholder,
    phoneErrorMessage,
    websiteLabel,
    websitePlaceholder,
    projectLabel,
    projectPlaceholder,
    projectErrorMessage,
    ctaButtonLabel,
  },
  headerIllustration {
    url,
    alt
  },
  title,
  emailLabel,
  phoneLabel,
  addressLabel,
}
