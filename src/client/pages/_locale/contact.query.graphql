query Contact($locale: SiteLocale, $altLocale: SiteLocale)  {
  contact(locale:$locale) {
    ...contact
  },
  alternateUrl: contact(locale:$altLocale) {
    slug
  }
}

fragment contact on ContactRecord {
  title
}
