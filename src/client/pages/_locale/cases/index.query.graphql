query ($locale: SiteLocale, $alternateLocale: SiteLocale)  {
  caseOverview(locale:$locale) {
    ...caseOverview
  },

  alternate:caseOverview {
    slug(locale:$alternateLocale)
  }
}

fragment caseOverview on CaseOverviewRecord {
  title
}
