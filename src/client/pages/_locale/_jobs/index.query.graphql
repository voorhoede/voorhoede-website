query ($locale: SiteLocale, $alternateLocale: SiteLocale)  {
  jobOverview(locale:$locale) {
    ...jobOverview
  },

  alternate:jobOverview {
    slug(locale:$alternateLocale)
  }
}

fragment jobOverview on JobOverviewRecord {
  title
}
