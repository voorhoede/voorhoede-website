query ($locale: SiteLocale, $slug: String)  {
  page: service(locale: $locale, filter: { slug: { eq: $slug } }) {
    ...page
  },

  alternate: service(locale: $locale, filter: { slug: { eq: $slug } }) {
    slug(locale: $locale)
  },

  alternateParent: serviceOverview {
    slug
  }
}

fragment page on ServiceRecord {
	slug,
  title,
  subtitle,
  headerIllustration {
    url,
    alt
  }
  items {
    title,
    body,
    image {
      url,
      alt
    }
  }
}
