query ($locale: SiteLocale)  {
  page: serviceOverview(locale:$locale) {
    ...page
  },
  alternate: serviceOverview(locale:$locale) {
    slug
  }
}

fragment page on ServiceOverviewRecord {
  title,
  slug,
  services {
    title,
    tagline,
    slug,
    summary {
      title
    }
  },
  getInTouchTitle,
  getInTouchForm {
    title,
    nameLabel,
    namePlaceholder,
    emailLabel,
    emailPlaceholder,
    summaryLabel,
    summaryPlaceholder,
    phoneLabel,
    phonePlaceholder,
    ctaButtonLabel
  }
}
